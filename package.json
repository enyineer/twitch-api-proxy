{
  "name": "twitch-proxy",
  "module": "src/index.ts",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --outfile twitch-proxy"
  },
  "type": "module",
  "dependencies": {
    "@inquirer/prompts": "^4.1.0",
    "open": "^10.0.4",
    "winston": "^3.11.0"
  }
}